import React, { useEffect } from "react";
import { View, TouchableOpacity, Text, Image, StyleSheet,TextInput,StatusBar,ImageBackground } from "react-native";


const AutoFinance = () => {

   
    return (

        <Text>AutoFinance</Text>
    
                   
    );
 };

 const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: "#fff",
    },
   
});

export default AutoFinance;

    